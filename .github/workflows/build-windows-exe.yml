name: Build Windows EXE

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'assets/**'
      - 'requirements.txt'
      - '.github/workflows/build-windows-exe.yml'
  workflow_dispatch:  # 手動実行も可能

permissions:
  contents: write   # GitHub Release を作成するために必要

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller `
            --onefile `
            --windowed `
            --name MikiApp `
            --add-data "assets;assets" `
            --collect-all PySide6 `
            src/main.py

      - name: Verify build
        run: |
          if (Test-Path dist/MikiApp.exe) {
            $size = (Get-Item dist/MikiApp.exe).Length / 1MB
            Write-Host "✅ MikiApp.exe built: $($size.ToString('0.0')) MB"
          } else {
            Write-Error "MikiApp.exe not found"
            exit 1
          }

      - name: Generate release tag
        id: tag
        shell: bash
        run: |
          TAG="build-$(date +%Y%m%d-%H%M%S)-${GITHUB_SHA:0:7}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Delete previous "latest" release (keep only the newest)
        continue-on-error: true
        run: |
          gh release delete latest --yes --cleanup-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release (latest)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Windows EXE Build"
          body: |
            自動ビルドされた Windows 実行ファイル

            - ビルド日時: ${{ github.event.head_commit.timestamp }}
            - コミット: ${{ github.sha }}
            - ファイル: `MikiApp.exe`

            ダウンロードして直接実行できます。
          files: dist/MikiApp.exe
          prerelease: false
          make_latest: true
